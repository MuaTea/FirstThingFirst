#include <stdio.h>
int main() {
    int num;
    scanf("%d",&num);

    int i;
    for (i=2;i<num;i++) {
        if (num%i==0)
            break;

        if(i==num)
            printf("%dYes",num);
        else
            printf("%dNo",num);
    }
}
